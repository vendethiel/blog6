#!/usr/bin/env perl6
use v6;
use Bailador;
use Bailador::Template::Mojo;

my $app = Bailador::App.current;
$app.renderer = Bailador::Template::Mojo.new;
$app.location = './';

get '/' => sub {
  template 'simple.tm', 'World';
};

baile;
